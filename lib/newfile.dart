import 'package:flutter/material.dart';

class Newfile1 extends StatelessWidget {
  const Newfile1({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
        body: Center(
          child: Text('Hello World! oown'),
        ),
      );
  }
}